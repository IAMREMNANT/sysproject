/**
 * Naming Conventions 
 * 
 * 변수이름을 지을 때, 꼭 지켜야 할 것
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수기호는 언더스코어와 달러만 사용할 수 있다.
 * 3) 숫자로 이름을 시작할 수 없다. ex) 1name, 2name
 * 4) 키워드는 변수명으로 사용할 수 없다. ex) var var = 'var';
 */

let codefactory = '코드팩토리';
var $ive = '아이브';
const _yoJin = '유진';

console.log($ive);
console.log(_yoJin);

//let 1name = 'no'
//let const = 'attention'

/** 
 * Naming conventions2
 * 
 * 1) camelCase - 변수명을 지을 때, 첫번째 단어는 소문자로 시작하고, 그 다음 단어부터는 첫글자를 대문자로 쓴다.
 * ex) camelCase - 대부분의 언어에서 많이 사용
 * 2) snake_case - 변수명을 지을 때, 모두 소문자로 쓰고, 단어 사이에 언더스코어(_)를 넣는다.
 * ex) 파일이름을 지을 때 등, 파이썬에서 자주 사용
 * 3) PascalCase - 변수명을 지을 때, 모든 단어의 첫글자를 대문자로 쓴다.
 * ex) C# 마이크로소프트 계열의 언어에서 사용함 / 자주사용하지 않음.
 * 
 */